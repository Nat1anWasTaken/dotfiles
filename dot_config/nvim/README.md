# Neovim config

This repository is meant to be cloned directly into `~/.config/nvim`.

## Install

```sh
git clone <your-repo-url> ~/.config/nvim
```

On first launch, `lazy.nvim` bootstraps itself automatically from `init.lua`.

## Update

```sh
git -C ~/.config/nvim pull
```
